import React, { ReactElement } from "react";
import styles from "./About.module.scss";

const About = (): ReactElement => {
  return <h1 className={styles.about}>Это суперуникальный проект по клешу</h1>;
};

export default About;
