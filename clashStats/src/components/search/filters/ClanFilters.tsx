import React, { ReactElement } from "react";
import FiltersContainer from "./FiltersContainer";

const ClanFilters = (): ReactElement => {
  return <FiltersContainer type={"clan"} />;
};

export default ClanFilters;
