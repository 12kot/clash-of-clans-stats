import React, { ReactElement } from "react";
import FiltersContainer from "./FiltersContainer";

const PlayerFilters = (): ReactElement => {
  return <FiltersContainer type={"player"} />;
};

export default PlayerFilters;
